spring:
  application:
    name: short-link
  profiles:
    default: local

  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true